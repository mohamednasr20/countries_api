import { makeStyles } from "@material-ui/core/styles";

const useStyles = makeStyles({
  bordersContainer: {
    marginTop: "2rem",
  },
  bordersTitle: {
    marginBottom: "1rem",
    marginTop: "0.5rem",
  },

  btn: {
    marginRight: "0.5rem",
    marginBottom: "0.5rem",
  },
});

export default useStyles;
