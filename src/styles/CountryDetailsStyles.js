import { makeStyles } from "@material-ui/core/styles";

const useStyles = makeStyles({
  grid: {
    marginTop: "3rem",
  },

  img: {
    maxHeight: 220,
    maxWidth: "100%",
  },

  bordersContainer: {
    marginTop: "2rem",
  },
  bordersTitle: {
    marginBottom: "1rem",
    marginTop: "0.5rem",
  },

  btn: {
    marginRight: "0.5rem",
    marginBottom: "0.5rem",
  },
  link: {
    textDecoration: "none",
  },
});

export default useStyles;
